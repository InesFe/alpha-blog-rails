<nav class="navbar navbar-expand-lg" style="background-color: #2E2E2E "; >
  <div class="container-fluid">
    <%=link_to "Alpha Blog", root_path, class: "navbar-brand", id: "logo"%>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
   
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item"><%= link_to "A propos", about_path, class: "nav-link"%></li>
        <li class="nav-item"><%= link_to "Articles", articles_path, class: "nav-link"%></li>
        <li class="nav-item"><%= link_to "Utilisateurs", users_path, class: "nav-link"%></li>
        </li>
       
      <% if logged_in? %>
        <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
            Profil
            <%= "(admin)" if current_user.admin? %>
          </a>
          <ul class="dropdown-menu">
            <li><%= link_to "Afficher mon profil", user_path(current_user), class: "dropdown-item"%></li>
            <li><%= link_to "Editer mon profil", edit_user_path(current_user), class: "dropdown-item"%></li>
            <li><a class="dropdown-item" href="#">Something else here</a></li>
          </ul>
        </li>
      </ul>
      
        <form class="d-flex" role="search">
          <input class="form-control me-2" type="search" placeholder="tut" aria-label="Search">
        </form>
        
        <%= form_tag(logout_path, method: :delete, data: {turbo: false}) do %>
          <button class="btn btn-outline-light" type="submit">Déconnexion</button>
        <% end %>
      
      <% else %>
        <li class="nav-item"><%= link_to "Se connecter", login_path, class: "nav-link" %></li>
        <li class="nav-item"><%= link_to "S'inscrire", signup_path, class: "nav-link" %></li>
      
      <% end %>
    </div>
  </div>
</nav>