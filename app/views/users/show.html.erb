<div class="container">
    <div class="center">
        <h1>Bienvenue <%=@user.username%></h1>
        <%= gravatar_for @user, size: 120%>
        <h4>Articles écrits par <%= @user.username%></h4>
    </div>

    <div class="pagination justify-content-center"><%= will_paginate @user_articles %></div>
        <%= render 'articles/articles', obj: @user_articles %>
    <div class="pagination justify-content-center"><%= will_paginate @user_articles %></div> 
</div>